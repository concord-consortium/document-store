# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  admin_name: Admin
  admin_email: admin@concord.org
  admin_password: changeme
  domain_name: localhost.localdomain
  secret_key_base: 152f3036f651712a6987e970e88912be370ba153f50fd9eebfeee412f88627920038edd05c1d4926bc02c738dcc0ba6fb0ab0d872456a91b8b2c9af4a436eb65

test:
  domain_name: example.com
  secret_key_base: 61a6fa38260a0fdfecac5c088aadd91e87be4f6cf9bafa9b67385b0b1b37a1d02555f85a5b31bf60b5a54a838be345663a614fa600279abf633ec8599560c1e5

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  admin_name: <%= ENV["ADMIN_NAME"] %>
  admin_email: <%= ENV["ADMIN_EMAIL"] %>
  admin_password: <%= ENV["ADMIN_PASSWORD"] %>
  domain_name: <%= ENV["DOMAIN_NAME"] %>
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
